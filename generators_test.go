package matreshka

import (
	"testing"
)

func Test_GenerateGoConfigKeys(t *testing.T) {
	//c, err := ParseConfig([]byte(fullConfig))
	//require.NoError(t, err)

	//keys, values, err := GenerateConfigKeys("matreshka", c)
	//require.NoError(t, err)
	//_, _ = keys, values
}
